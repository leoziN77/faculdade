#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){
	setlocale(LC_ALL, "Portuguese");
    float salario, novoSalario;

    printf("Digite o valor do salário: ");
    scanf("%f", &salario);

    if(salario <= 3000)
    {

    }
    else
    {
        
    }

    return 0;
}